{% extends 'base.html.twig' %}

{% block title %}FicheClient{% endblock %}

{% block body %}
    <h1>FicheClient</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fiche_client.id }}</td>
            </tr>
            <tr>
                <th>Nom_client</th>
                <td>{{ fiche_client.nomClient }}</td>
            </tr>
            <tr>
                <th>Tel_client</th>
                <td>{{ fiche_client.telClient }}</td>
            </tr>
            <tr>
                <th>Email_client</th>
                <td>{{ fiche_client.emailClient }}</td>
            </tr>
            <tr>
                <th>Raison_sociale</th>
                <td>{{ fiche_client.raisonSociale }}</td>
            </tr>
            <tr>
                <th>Activite</th>
                <td>{{ fiche_client.activite }}</td>
            </tr>
            <tr>
                <th>Site_web</th>
                <td>{{ fiche_client.siteWeb }}</td>
            </tr>
            <tr>
                <th>Referencement</th>
                <td>{{ fiche_client.referencement }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('fiche_client_index') }}">back to list</a>

    <a href="{{ path('fiche_client_edit', {'id': fiche_client.id}) }}">edit</a>

    {{ include('fiche_client/_delete_form.html.twig') }}
{% endblock %}
